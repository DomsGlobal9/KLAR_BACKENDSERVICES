export interface FlightFilterOptions {
  flightId?: string; 
  airlineCode?: string;
  flightNumber?: string;
  classType?: string; 
  fareIdentifier?: string; 
  minPrice?: number;
  maxPrice?: number;
  departureTime?: string; 
  airlineName?: string; 
}
